#include "main.h"

/**
 * print_array - Prints n elements of an array
 * @a: char
 * @n: char
 * Return: Always 0.
 */

void print_array(int *a, int n)
{
int b;

for (b = 0; b < (n - 1); b++)
{
printf("%d, ", a[n]);
}
if (i == (n - 1))
{
printf("%d", a[n - 1]);
}
printf("\n");
}
